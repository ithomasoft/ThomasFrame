<resources>

    <!-- Base application theme. -->
    <style name="thomas_AppTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/thomas_color_app_background</item>
    </style>
</resources>
